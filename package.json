{
  "name": "wowohcool-web",
  "private": true,
  "scripts": {
    "build": "npx esbuild main.src.js --bundle --minify --outfile=main.js",
    "watch": "npx esbuild main.src.js --bundle --minify --outfile=main.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
